# Get ~/.bashrc
# Get ~/.config/nvim/init.vim
sudo apt-get update && sudo apt-get install \
    neovim \
    git-lfs \
    ripgrep \
    fzf \
    tmux \
    tree
# neovim setup commands pip install neovim :Update.. :check.. :Plug...

# Docker Ubuntu apt-get setup https://docs.docker.com/engine/install/ubuntu/#install-using-the-repository
# docker-compose standalone https://docs.docker.com/compose/install/other/#install-compose-standalone
# Docker permissions https://docs.docker.com/engine/install/linux-postinstall/#manage-docker-as-a-non-root-user

# https://www.rust-lang.org/learn/get-started
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
git config..
